import { SafeAreaView, StyleSheet, Text } from "react-native";

export default function MaterialHomeScreen() {
  return (
    <SafeAreaView>
      <Text>Material Redirecting</Text>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});
